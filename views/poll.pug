extends layout

block content
  h1= title
  
  div 
    h2 #{question}
    form(method="post", action="")
      ul
        each ans in answers
          li #{ans.text}
            input(type="radio", name="answer" value=ans.text) 
        else
          p No answers.
      input(type="submit")
